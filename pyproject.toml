[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ha-carson-living"
version = "0.1.0"
description = "Home Assistant custom integration for Carson Living"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.12"
dependencies = [
  "carson_living @ git+https://github.com/pbrink231/python-carson-living@main",
]

[project.optional-dependencies]
test = [
  "homeassistant==2024.12.3",
  "pytest-homeassistant-custom-component==0.13.193",
  "aiohttp-cors==0.7.0",
  "requests-mock==1.12.1",
  "pytest==8.3.3",
  "pytest-cov==6.0.0",
  "coveralls==4.0.1",
  "flake8==7.1.1",
  "pylint==3.3.4",
]

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
include = ["custom_components*"]
namespaces = true

[tool.setuptools.package-data]
"custom_components.carson_living" = ["*.json", "*.yaml", ".translations/*.json"]

[tool.pytest.ini_options]
addopts = "--force-enable-socket"
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
norecursedirs = [".git"]

[tool.coverage.run]
branch = true
omit = [
  "*/__main__.py",
  "*/tests/*",
  "*/venv/*",
]

[tool.coverage.report]
omit = [
  "*/python?.?/*",
  "*/site-packages/nose/*",
  "*__init__*",
]

[tool.pylint.format]
max-line-length = 120

[tool.pylint."messages control"]
disable = [
  "too-many-arguments",
  "too-many-instance-attributes",
]
